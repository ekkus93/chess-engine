name: Fix Task 15.5 import

on:
  push:
    branches:
      - rust-engine
    paths:
      - ".github/workflows/fix-task-15-5-import.yml"

permissions:
  contents: write

concurrency:
  group: fix-task-15-5-import
  cancel-in-progress: false

jobs:
  fix:
    name: Apply Task 15.5 import fix
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rust-engine
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Apply import fix
        run: python3 scripts/fix_task_15_5_import.py "$GITHUB_WORKSPACE"

      - name: Remove temporary fix controls
        run: |
          set -euo pipefail
          git rm \
            .github/workflows/fix-task-15-5-import.yml \
            scripts/fix_task_15_5_import.py

      - name: Commit clean import fix
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/chess-search/src/transposition/store.rs
          git diff --cached --check
          git commit -m "fix: scope TT replacement test import"
          git push origin HEAD:rust-engine
