name: CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  registration:
    name: CI registration stub
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Explain branch-specific CI
        run: |
          echo "The authoritative CI implementation is defined on the rust-engine branch."
          echo "Dispatch this workflow with ref=rust-engine to validate the Rust workspace."
