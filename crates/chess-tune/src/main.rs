#![forbid(unsafe_code)]
//! Offline evaluation dataset, tuning, and candidate-validation tooling.
//!
//! Tuning artifacts are versioned inputs and outputs; this crate must not
//! introduce automatic runtime learning or implicit weight loading.

fn main() {}
